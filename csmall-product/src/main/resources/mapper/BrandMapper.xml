<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.hqu.csmall.product.mapper.BrandMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO pms_brand(name, pinyin, logo, description, keywords, sort, enable,
                              sales, product_count, comment_count, positive_comment_count,
                              gmt_create, gmt_modified)
        VALUES(#{name}, #{pinyin}, #{logo}, #{description}, #{keywords}, #{sort}, #{enable},
               #{sales}, #{productCount}, #{commentCount}, #{positiveCommentCount},
               #{gmtCreated}, #{gmtModified})
    </insert>

    <select id="list" resultType="cn.hqu.csmall.product.pojo.vo.BrandListItemVO">
        SELECT id, name, pinyin, logo, keywords, sort, enable, sales, product_count
        FROM pms_brand ORDER BY sort
    </select>

    <select id="getStandardById" resultType="cn.hqu.csmall.product.pojo.vo.BrandStandardVO">
        SELECT id, name, pinyin, logo, description, keywords, sort, enable, sales,
               product_count, comment_count, positive_comment_count
        FROM pms_brand WHERE id=#{id}
    </select>

    <select id="search" resultType="cn.hqu.csmall.product.pojo.vo.BrandListItemVO">
        SELECT id,name,pinyin,enable,sort
        FROM pms_brand WHERE 1=1
        <if test="name != null and name != ''">
            AND name = #{name}
        </if>
        <if test="id != null">
            AND id = #{id}
        </if>
        ORDER BY sort
    </select>

</mapper>
