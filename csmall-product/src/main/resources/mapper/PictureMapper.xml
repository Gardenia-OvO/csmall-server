<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.hqu.csmall.product.mapper.PictureMapper">

    <select id="listByAlbumId" resultType="cn.hqu.csmall.product.pojo.vo.PictureListItemVO">
        SELECT id, album_id AS albumId, url, description, is_cover AS isCover, sort
        FROM pms_picture
        WHERE album_id = #{albumId}
        ORDER BY sort ASC, id ASC
    </select>

</mapper>
