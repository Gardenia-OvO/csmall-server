<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.hqu.csmall.passport.mapper.AdminRoleMapper">

    <insert id="insertBatch">
        insert into ams_admin_role(admin_id,role_id,gmt_create,gmt_modified)
        values
        <foreach collection="array" item="adminRole" separator=",">
            (#{adminRole.adminId},#{adminRole.roleId},#{adminRole.gmtCreate},
            #{adminRole.gmtModified})
        </foreach>
    </insert>

</mapper>
