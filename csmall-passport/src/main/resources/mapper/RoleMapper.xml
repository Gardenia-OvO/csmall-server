<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.hqu.csmall.passport.mapper.RoleMapper">

    <select id="list" resultType="cn.hqu.csmall.passport.pojo.vo.RoleListItemVO">
        SELECT id, name, description, sort, enable
        FROM ams_role ORDER BY sort
    </select>

</mapper>
